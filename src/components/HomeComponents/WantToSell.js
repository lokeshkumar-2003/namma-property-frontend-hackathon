import React, { useState } from "react";
import WebsiteWrapper from "./WebsiteWrapper";
import PropertySubmissionForm from "./PropertySellSubmission";

const WantToSell = () => {
  return (
    <WebsiteWrapper>
      <PropertySubmissionForm />
    </WebsiteWrapper>
  );
};

export default WantToSell;
